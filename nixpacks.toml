[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "gcc", "pkg-config", "mysql80"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip", "python3 -m pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT"